spring:
  profiles:
    group:
      local:
        - local-mysql
        - local-token
        - local-image
      dev:
        - dev-mysql
        - dev-token
        - dev-image
      test:
        - test-mysql
        - local-token
        - local-image
    active: ${PROFILE}
