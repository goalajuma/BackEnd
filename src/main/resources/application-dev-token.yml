spring:
  config:
    activate:
      on-profile: dev-token

security:
  jwt:
    token:
      secretKey: ${security.key}
      access:
        validTime: 2629800000
      refresh:
        validTime: 5259600000
